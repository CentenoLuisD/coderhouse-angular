import { NamelastnamePipe } from './namelastname.pipe';

describe('NamelastnamePipe', () => {
  it('create an instance', () => {
    const pipe = new NamelastnamePipe();
    expect(pipe).toBeTruthy();
  });
});
