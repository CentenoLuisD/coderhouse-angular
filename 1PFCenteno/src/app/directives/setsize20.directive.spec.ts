import { Setsize20Directive } from './setsize20.directive';

describe('Setsize20Directive', () => {
  it('should create an instance', () => {
    const directive = new Setsize20Directive();
    expect(directive).toBeTruthy();
  });
});
